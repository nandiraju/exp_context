import { StyleSheet, Text, View } from "react-native";
import React from "react";
import TodoApp from "./SimpleToDo";

const Settings = () => {
  return (
    <View className="flex-1 justify-center items-center bg-blue-50">
      <TodoApp />
    </View>
  );
};

export default Settings;
